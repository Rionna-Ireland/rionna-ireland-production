{
	"name": "@repo/scripts",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"clean": "git clean -xdf .cache .turbo dist node_modules",
		"create:user": "dotenv -c -e ../../.env -- tsx ./src/create-user.ts",
		"circle:spike": "dotenv -c -e ../../.env.staging -- tsx ./src/circle-content-spike.ts",
		"type-check": "tsc --noEmit"
	},
	"dependencies": {
		"@repo/auth": "workspace:*",
		"@repo/database": "workspace:*",
		"@repo/logs": "workspace:*",
		"@repo/utils": "workspace:*"
	},
	"devDependencies": {
		"@repo/tsconfig": "workspace:*",
		"@types/node": "catalog:",
		"nanoid": "catalog:",
		"tsx": "catalog:"
	}
}
