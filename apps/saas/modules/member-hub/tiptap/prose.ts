/** One prose theme shared by the reader, the admin preview, and (via editorProps) the editor. */
export const CIRCLE_PROSE_CLASS =
	"prose prose-neutral dark:prose-invert max-w-none prose-headings:font-display prose-headings:font-medium";
